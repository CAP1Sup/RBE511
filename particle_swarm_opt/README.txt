Please install the requirements.txt file using the following command: "pip install -r requirements.txt"

This code can be run by executing the "pso.py" file in Python.

The inertia strategy used can be changed by modifying the "inertia_strategy" variable in the "pso.py" file on line 97.