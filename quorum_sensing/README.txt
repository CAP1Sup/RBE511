Please install the requirements.txt file using the following command: "pip install -r requirements.txt"

This code can be run by executing the "sensing.py" file in Python.

The results of the simulations will be saved as under the "plots" folder in this directory.